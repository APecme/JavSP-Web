# JavSP WEB v1.1.34

## 新增功能

- STRM 文件刮削开关

## 功能介绍

- **STRM 文件刮削开关**：在爬虫预设的扫描器配置中新增“STRM 忽略最小文件大小”开关。开启后，小于最小匹配文件大小的 `.strm` 文本文件仍可进入任务队列、识别番号并执行刮削；其他视频格式继续遵守原有的最小文件大小限制。

## 优化与修复

- Web 任务创建层与 JavSP 扫描层统一应用 STRM 大小豁免规则，避免 `.strm` 文件在任一层被提前过滤。
